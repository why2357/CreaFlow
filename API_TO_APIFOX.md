# 将 API 导入 Apifox

本项目已自动生成 OpenAPI 规范文件，可导入 Apifox 进行 API 管理。

## 导入步骤

### 1. 打开 Apifox

登录 [Apifox](https://apifox.com/)

### 2. 创建新项目（或使用现有项目）

### 3. 导入 OpenAPI 文件

点击项目设置 → **导入数据** → 选择 **OpenAPI** 格式

### 4. 上传文件

选择项目根目录下的 `openapi.json` 文件

### 5. 确认导入

检查导入配置，点击**确认导入**

## 已生成的接口统计

| 模块        | 接口数量                                                                     |
| ----------- | ---------------------------------------------------------------------------- |
| auth        | 认证相关                                                                     |
| demo        | 测试模块                                                                     |
| model       | 模型管理                                                                     |
| monitor     | 监控模块 (缓存、登录日志、在线用户、操作日志)                                |
| system      | 系统管理 (客户端、配置、部门、字典、菜单、通知、OSS、岗位、角色、租户、用户) |
| tool        | 工具模块 (代码生成、文件上传)                                                |
| transaction | 交易模块                                                                     |
| workbench   | 工作台 (剧集、历史记录、素材库、成员、项目、项目数据、故事板)                |

**总计：35 个模块，98 个接口路径，114 个接口方法**

## 重新生成

如需更新 API 文档，运行：

```bash
node scripts/generate-openapi.js
```

生成后的文件：`openapi.json`

## Apifox 功能建议

导入后，你可以使用 Apifox 的以下功能：

1. **接口调试** - 直接在 Apifox 中测试接口
2. **Mock 服务** - 为前端提供 Mock 数据
3. **接口文档** - 分享给团队成员
4. **自动化测试** - 创建测试用例集合
5. **代码生成** - 生成各种语言的请求代码
